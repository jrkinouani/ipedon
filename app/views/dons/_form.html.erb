     <%= simple_form_for(@don, html: { class: 'form-horizontal', id: "add_plat" }) do |f| %>
      <%= f.text_field :pseudo , :placeholder => "Votre nom de Famille", class:"form-control"%>
      <%= f.text_field :price , :placeholder => "Votre don en euros", class:"form-control"%>
      <%= f.submit :value => "Validez",  :class =>"btn btn-secondary btn-lg btn-block" %>
    <% end%>
